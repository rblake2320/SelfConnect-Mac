# Core SDK dependencies (all platforms)
Pillow>=10.0.0
psutil>=5.9.0

# Windows-only: UIA text extraction (get_text_uia, approval_partner, antigravity_controller)
pywinauto>=0.6.8; sys_platform == "win32"
comtypes>=1.4.0; sys_platform == "win32"

# macOS: the selfconnect_mac v2 package needs the pyobjc/iterm2/atomacos stack.
# Install it via the packaged extra instead of listing it here:
#   pip install -e ".[mac]"        # Mac capability lanes
#   pip install -e ".[mac,test]"   # + test suite deps (fastapi, pytest, ...)
# See pyproject.toml [project.optional-dependencies] for the authoritative list.

# Optional: OCR-based delivery verification (needed for verify_delivery OCR path)
# pytesseract>=0.3.10

# Optional: Telegram approval bridge (needed for approval_telegram.py)
# python-dotenv>=1.0.0
# python-telegram-bot>=22.0

# Optional: vision_server FastAPI service — see requirements-vision.txt
